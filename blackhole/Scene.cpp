#include "Scene.h"
#include <cmath>
#include "Constants.h"
using namespace glm;

CScene::CScene() //: blackhole(dvec3(0, 0, 0), BLACKHOLE_MASS, ACCREATION_DISK_COEF)
{
    //disk = AccreationDisk(blackhole.m_radius * blackhole.m_accreation_disk_coef);
}